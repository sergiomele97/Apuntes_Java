package ConceptosJavaBasico;

public class ErroresMain {

    public static void main(String[] args) {
        // Un error es un comportamiento inesperado en el programa

    /*
        3 tipos de errores:

            1. Error en tiempo de ejecución: Una situacion que ha ocurrido en neustro programa apra la cual no
            estabamos preparados y que hace que el programa aborte

        Ocurre durante la ejecucion del programa
        Ejemplo:
     */
        // System.out.println(5 / 0)

    /*
        2. Error de compilación: Error sintáctico
        Ejemplo: nos dejamos un ;
     */

    /*
        3. Error logico / error humano
        Lo que yo le digo al programa esta mal pensado

        Ejemplo:
        int temperatura = 15;
        if (temperatura >= 25) {    Sería un <= para tener sentido
        System.out.println("Hace frio!");
        }
     */
    }

}
